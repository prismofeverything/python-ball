import pygame
import utility
import interface

def okay():
    ak = interface.Interface((300, 300))
    ak.drawText((50, 50, 100, 100), utility.Color((1.0, 0.9, 0.2)), "what the fuck?")
    pygame.display.update()

    return ak
